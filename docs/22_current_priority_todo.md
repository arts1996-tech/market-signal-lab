# 22 現行の最優先開発ToDo

- 確定日: 2026-08-16
- 対象: フェーズ1〜4の残課題、前向き仮想口座、検証、運用・セキュリティ
- 位置づけ: 今後の実装順序と未完了項目を管理する唯一の正本

## 1. 優先順位と更新ルール

この文書は、2026-08-16に仕様、コード、ジョブ、画面、運用、テストを再照合した結果である。現行の未完了ToDoと再開順序は本書を正本とする。

- `docs/17_remediation_todo.md`は2026-07-12是正会議の完了済み履歴であり、現行ToDoではない。
- `docs/19_investment_decision_readiness_assessment.md`の評価と禁止範囲は維持するが、開発順序は本書を優先する。
- `docs/21_forward_shadow_operations.md`は実行主体の運用手順であり、機能完成度や開発順序は本書を優先する。
- 過去文書の「次の作業」「再開順序」「未完了」が本書と重複または矛盾する場合、本書へ統合済みとして扱う。
- 項目を完了にする場合は、コード、テスト、画面またはジョブ確認、文書更新、既知の制約の記録をすべて満たす。
- Raspberry Piの状態を変える配置、DB変更、timer・cron変更は、Macでの検証後にユーザーの明示承認を得て行う。

## 2. 先に解消する現状の矛盾

### C-1 前向き観察の名称と実体

現行の`run_forward_shadow.py`は不変JSONを日次保存できるが、入力は将来の保有期間分が存在する過去候補を使った再評価である。当日時点の新規シグナル、未決済保有、現金を翌営業日へ引き継ぐ正式な前向き仮想口座ではない。

推奨対応:

- 現行経路を削除せず、完成までは「遅延価格による研究スナップショット」と明記する。
- 当日時点シグナル生成と継続口座を別経路で実装する。
- 正式な前向き運用期間へ算入するのは、新経路の品質ゲート完了後に固定した最初の営業日以降だけとする。

### C-2 短期・中期口座要件と通常モード

デモは短期・中期各250万円だが、通常モードは`live_long_only`の1口座である。

推奨対応:

- 通常モードも短期・中期各250万円の独立口座にする。
- 口座ごとにルール、検証系列、現金、保有、損益、監査IDを分離する。

### C-3 ロードマップ表現と通常ジョブ

通常の`run_backtest.py`と`run_mid_term_analysis.py`はplaceholderでありながら`success`を記録する。

推奨対応:

- 未実装またはデータ不足を成功扱いしない。
- `insufficient_data`、`skipped`、`error`、`success`を区別し、実サービスへ接続する。

### C-4 Macスケジュールとデータ所在

Raspberry Piが収集の正本DBを持つ一方、Mac LaunchAgentはMacへ最後に復元したDBを使用し、自動同期しない。

推奨対応:

- Macの現行実行は検証用・研究用と表示する。
- 正式運用はRaspberry Piへ単独移行し、Macと二重に正式実行しない。
- Raspberry Pi停止時にMacの古いDBから正式記録を代替生成しない。

### C-5 秘密情報方針と開発用既定値

`.env.example`の秘密値を空にする方針に対し、DBパスワードを含む既定接続文字列が存在する。

推奨対応:

- `.env.example`の秘密値を空にする。
- 必須値の未設定を起動前検証し、分かりやすく停止する。
- ログ、例外、テスト、バックアップへ秘密値が出ないことをテストする。

## 3. NOW-P0: 正式な前向き仮想口座

データ30営業日の蓄積を待たず、Macの決定論的な合成データで完成させる。以下を完了するまで、現在価格・ニュース・LLMを使う判断機能へ進まない。

- [x] **NOW-P0-1 当日時点シグナル生成を過去評価から分離する**
  - `generate_signals_as_of`相当の境界を設ける。
  - 将来価格が存在しなくても、判断、見送り、データ不足を生成する。
  - 将来リターン、決済価格、結果ラベルをシグナル入力へ渡さない。
  - 判断時刻以前の価格、財務、市場情報だけを使用するテストを追加する。
  - 2026-08-16完了。`app/analysis/signal_generation.py`に専用境界を追加し、`price_time`と`available_at`（旧・デモ入力は`fetched_at`）を判断時刻で切り詰めてから計算する。買い候補、待機、データ不足を別状態で返し、将来結果フィールドの混入を禁止した。通常画面でも過去評価と別欄に表示する。Docker上の全181テスト成功を確認した。

- [ ] **NOW-P0-2 短期・中期の正式な独立口座を実装する**
  - 各口座の初期資金は250万円（JPY）。
  - 口座間の資金移動を行わない。
  - 短期・中期で利益確定、損切り、最大保有期間、戦略版を分離する。
  - 現金、保有、実現／未実現損益、累積損益、最大ドローダウンを翌営業日へ引き継ぐ。

- [ ] **NOW-P0-3 追記専用の口座台帳と日次状態を永続化する**
  - 口座、判断、約定予定、約定、決済、見送り、日次残高を追記型で保存する。
  - 同じ口座・営業日・入力版の再実行は冪等にし、異なる後発入力による置換を拒否する。
  - JSONは監査用エクスポートとして残し、JSONだけを唯一の口座状態にしない。
  - DBマイグレーションはMacで作成・往復検証し、Raspberry Piへは承認前に適用しない。

- [ ] **NOW-P0-4 遅延研究系統と現在判断系統をデータ上も分離する**
  - `decision_track`を`delayed_historical`と`current_market`に分ける。
  - 観察日時、価格最終日、遅延日数、取得元、入力ハッシュ、品質ゲート結果を保存する。
  - 候補なし、履歴不足、価格欠損、鮮度不合格等の理由を個別に保存する。
  - J-Quants Freeの遅延価格による記録を現在の短期判断として表示しない。

- [ ] **NOW-P0-5 前向き記録の運用監視を追加する**
  - 短期・中期の当日記録、最終成功、3回の再試行失敗、欠測営業日を監視する。
  - Docker・DB停止、保存先容量不足、JSON欠落・改変を区別する。
  - `JobRun`へ開始、成功、見送り、失敗を保存する。
  - システム管理画面で警告と最終成功を確認できるようにする。

## 4. NOW-P1: 実行入口とフェーズ3の正常化

- [ ] **NOW-P1-1 通常バックテストジョブを実装する**
  - placeholderと偽の`success`を廃止する。
  - 実データ不足時は理由付き`insufficient_data`とする。
  - データ到達後は短期・中期を別系列としてウォークフォワード評価する。
  - 入力版、ルール版、検証期間登録、ベンチマーク、費用を記録する。

- [ ] **NOW-P1-2 中期分析ジョブを実装する**
  - 開示日時点の財務スナップショットだけを選択する。
  - 売上・営業利益・EPS成長、営業利益率、ROE、自己資本比率、営業CFを扱う。
  - 3・6・12か月モメンタム、52週高値乖離を、必要な連続価格がある場合だけ計算する。
  - データ不足、古さ、通貨・単位不明を警告する。

- [ ] **NOW-P1-3 財務画面と来歴の不整合を修正する**
  - `details`にある`book_value_per_share`を安全に読み、取得できる場合だけPBRを計算する。
  - ROEと営業利益率の比率を正しい百分率で表示する。
  - source、取得時刻、開示時刻、期間末、通貨、単位を表示する。
  - 未取得値はNULLのままとし、推測しない。

- [ ] **NOW-P1-4 全対象銘柄のバッチ分析と画面表示を分離する**
  - バッチ分析は品質ゲート通過全銘柄を対象にする。
  - 現在の10銘柄・200銘柄制限を分析母集団ではなくUI表示上限に限定する。
  - 分析結果を版付きで保存し、画面はページングまたは絞り込みで読む。
  - 件数制限で特定銘柄だけが恒常的に除外されないことをテストする。

- [ ] **NOW-P1-5 財務・SEC・ETFジョブを共通サービスへ統合する**
  - 入力検証、Repository、冪等保存、取得ログ、JobRun、失敗分類を共通化する。
  - J-Quants／SECの単銘柄明示実行を維持する。
  - 自動全銘柄収集は無料条件、規約、レート制限を公式再確認してから承認を得る。

## 5. NOW-P1: 約定モデルの追加安全策

- [ ] **NOW-P1-6 企業行動モデルと保守的ゲートを追加する**
  - 株式分割・併合、配当落ち、現金配当、権利日・支払日を表現する。
  - 合併・株式交換等を未対応イベントとして識別する。
  - 企業行動を確認できない期間は、結果へ警告するか評価を見送る。
  - 合成イベントによる口座・価格調整テストを追加する。

- [ ] **NOW-P1-7 銘柄ライフサイクルとサバイバーシップ対策を追加する**
  - 上場日、上場廃止日、時点別市場・業種、投資可能状態を保持する。
  - 過去時点の銘柄集合を復元できるようにする。
  - 上場廃止時の保守的な評価・決済規則を版管理する。
  - 現在残存銘柄だけで過去検証しないテストを追加する。

- [ ] **NOW-P1-8 多通貨・為替会計を実装する**
  - USD建て損益、JPY換算損益、為替寄与を分離する。
  - 取引時・評価時USD/JPY、為替スプレッド、為替コストを記録する。
  - 為替欠損時は暗黙補完せず、約定または評価を見送る。
  - 合成USD資産と合成為替で先にテストする。

- [ ] **NOW-P1-9 税モデルの扱いを明確化する**
  - 当面は税率0%と税引前評価を明示する。
  - 現行の単純税率を実際の損益通算・税務再現と表示しない。
  - 将来の税モデルは別バージョンとし、口座種別・損益通算等の仕様承認後に実装する。

## 6. NOW-P2: 検証能力と監査

- [ ] **NOW-P2-1 実データ用ウォークフォワード実行器を完成する**
  - 学習期間、検証期間、前向き期間を分離する。
  - 凍結後ルールだけを未使用期間へ適用する。
  - 異なるルールによる同一検証期間の再利用を拒否する。

- [ ] **NOW-P2-2 相場局面別・属性別評価を追加する**
  - 上昇・下落・横ばい、高・低ボラ、円高・円安を区分する。
  - 業種、流動性、スコア帯別に結果を集計する。
  - 少数標本は成績判定せず、標本数と信頼区間を表示する。

- [ ] **NOW-P2-3 ベンチマーク比較を拡充する**
  - 日経平均、取得可能なTOPIX、対象ETF、単純保有、現金保有を候補とする。
  - 同一期間、同一通貨、費用控除後で比較する。
  - 利用できない指数や価格を推測しない。

- [ ] **NOW-P2-4 感度分析と頑健性検査を追加する**
  - 利益確定、損切り、スコア閾値、費用、スリッページを小幅に変える。
  - 最良パラメータ探索ではなく、結果の脆弱性確認として扱う。
  - 多重比較と過学習を警告する。

- [ ] **NOW-P2-5 監査記録の改変・欠落検知を追加する**
  - ファイルハッシュ、前レコードハッシュ、連番または同等の追記監査を持つ。
  - 手動変更、削除、順序欠落を検出する検証ジョブを追加する。
  - 監査異常を投資結果とは分離してシステム管理へ表示する。

## 7. NOW-P2: 既存分析仕様の残実装

次の項目は仕様に存在するが、未実装、未接続、または限定実装である。取得元に値がない場合は推測しない。

- [ ] 週次・月次リターン
- [ ] ATRの通常銘柄分析への接続
- [ ] ストキャスティクス
- [ ] 支持・抵抗候補
- [ ] ブレイクアウト
- [ ] ベンチマーク・業種への相対強度
- [ ] 52週高値からの乖離
- [ ] ETFのNAV乖離、トラッキングエラー、構成銘柄、流動性指標
- [ ] 日米波及の多変量回帰
- [ ] 仕様どおりの7段階米国騰落条件
- [ ] イベントスタディ
- [ ] 相場局面別の日米波及分析
- [ ] NASDAQ 100、SOX、VIX、米国10年債利回り、USD/JPY等の説明変数
- [ ] 利用可能な場合の日本側最大変動、ギャップフィル、寄り付き・終値方向一致

## 8. NOW-P2: セキュリティと運用

- [ ] **NOW-P2-6 秘密情報の既定値を廃止し、マスキングをテストする**
- [ ] **NOW-P2-7 Streamlitの公開アドレスを環境別に設定する**
- [ ] **NOW-P2-8 前向きジョブと重い分析ジョブの多重起動を防止する**
- [ ] **NOW-P2-9 ログローテーション、欠測、失敗数を運用画面へ統合する**
- [ ] **NOW-P2-10 Raspberry Pi用systemd service／timerテンプレートをMacで検証する**
- [ ] **NOW-P2-11 MacからRaspberry Piへの実行主体切替に単一所有者チェックを追加する**
- [ ] **NOW-P2-12 バックアップの定期リストア検証を自動化する**

## 9. 外部条件がそろうまで完成扱いにしない項目

以下は設計、インターフェース、合成データテストまでは進められるが、外部条件なしに本番完成とはしない。

- 現在価格を使う1〜20営業日の短期判断
- 米国個別株・米国ETFの本番価格収集
- 実際の板、特別気配、値幅制限、取引停止データ
- 過去ニュースの公表・取得・利用可能時刻をそろえた時点整合データ
- 予測能力、較正、手数料控除後の収益優位性の証明
- 6〜12か月程度の正式な前向き仮想運用
- Gemini、Claude、ChatGPT等のLLM連携
- Slack連携

価格・ニュース・LLM等の外部サービスを選ぶ際は、公式情報で無料枠、料金、利用規約、表示権、再配布条件、レート制限を再確認し、ユーザー承認後に導入する。

## 10. 完了条件と再開時の提示

各実装単位で次を満たす。

1. Macの合成データまたは隔離DBで正常系・異常系をテストする。
2. 全pytestを成功させる。
3. 通常／デモ、遅延研究／現在判断を混在させない。
4. 起動、確認、障害時の見方をREADMEへ記載する。
5. 秘密情報、ログ、バックアップ、DBデータがGit対象にないことを確認する。
6. Raspberry Piへ影響する場合は、変更対象、バックアップ、停止時間、ロールバックを提示して承認を得る。
7. 完了した項目はチェックし、証拠となるテスト、コミット、運用確認を追記する。

再開時に最初に提示するタスクは`NOW-P0-2`である。`NOW-P0-2`から`NOW-P0-5`までを一つの前向き仮想口座品質ゲートとして進める。残タスクは本書の未チェック項目を上から順に提示する。
