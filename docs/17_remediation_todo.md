# 17 評価会議後の是正ToDo

2026-07-12の開発・評価・テスト／運用・投資分析・販売／プロダクト合同レビューに基づくToDoです。
各項目は完了条件を満たすまで次の依存タスクへ進めません。Raspberry Piの状態を変える作業は、Macでの検証と利用者の明示承認後に行います。

## P0: 分析データの信頼性

- [x] **P0-1 サンプルデータを通常分析から隔離する**
  - 通常のCompose起動でサンプルを自動投入しない。
  - Repository境界で`sample`を既定除外し、デモモードは`sample`だけを明示利用する。
  - 既存のsample行は削除せず、通常分析・通常画面から参照しない。
  - 完了条件: source混在の単体テスト、通常／デモ手順のREADME記載、pytest成功。

- [x] **P0-2 分析入力のデータソース優先順位と旧結果の扱いを確定する**
  - 指数、株式、ETFごとに許容sourceと優先順位を決め、同一資産・時点の複数実sourceを決定論的に解決する。
  - source混在の可能性がある既存の相関・波及結果を「要再計算」として扱う方針を実装する。
  - 完了条件: 入力source・優先順位・再計算要否を結果と画面に表示する。

  実装済み: `source_priority_v1`として、指数・為替はFRED、日本株・日本ETFはJ-Quantsのみを許可する。未登録sourceへの暗黙フォールバックは行わない。選択済み入力のSHA-256、source方針、来歴、暫定の価格基準を結果に保存し、旧結果は`legacy-unknown`／`requires_recalculation`として保持する。

- [x] **P0-3 前営業日・価格基準を正しく判定する**
  - JPX・米国のセッション連続性を確認し、欠測をまたぐ値を日次リターンとして計算しない。
  - 株式分割・併合に対応する調整済みOHLCと価格基準を保存・選択する。
  - 完了条件: 休日、連休、夏時間、欠測、分割の回帰テストが通る。

- 完了: `exchange_calendars`による公式セッション判定、古い日付の範囲外スキップ、休日・連休・夏時間・欠測・分割調整の回帰テスト、JPXセッション基準の収集候補日、Pi配置と稼働確認を完了。`calendar_gap_report`は運用確認ジョブとして利用可能。

## P1: J-Quants収集と運用安全性

- [x] **P1-1 API失敗と恒久的なデータなしを分離する**
  - 401／403、429、5xx、ネットワーク障害、入力不正、確定したデータなしを別状態にする。
  - 一時障害を`unavailable`として永続化しない。
  - 日付全体の休場判定を単一銘柄に依存しない。
  - 完了条件: エラー分類・再試行・進捗再開のテストが通る。

  優先度引上げ: 2026-07-12のRaspberry Pi読み取り確認で、J-Quantsの429が`unavailable`として蓄積していることを確認した。以後のデータ損失を防ぐため、P0-3より先に実装・Mac検証する。

  完了: Mac側テストと、2026-07-12のRaspberry Pi配置・実運用確認を完了。null価格のHTTP 200応答は個別銘柄の`no_data`として保存され、日付全体は除外されないことを確認した。429・5xxは単体テストで`retry_pending`へ分離済みであり、実ログは継続監視する。

- [x] **P1-2 J-Quantsの期間取得方式を再設計する**
  - 公式のFree plan条件、期間指定上限、ページング、レート制限を実装直前に再確認する。
  - 現行の「1銘柄×1日」方式の実測を基準に、「1銘柄×期間」方式を比較・採用判断する。
  - 最新日を全銘柄で埋めた後は、未取得日を古い順に補完する。
  - 完了条件: 全銘柄・対象期間の見積りと、小規模実測の結果を文書化する。

  実装済み: 全銘柄の最新取得可能日を候補リストの先頭に固定し、その完了後は履歴日を古い順に補完する順序へMac側で修正した。J-Quantsのページングキーを追跡する期間取得基盤も追加した。Macで86970・2026-04-01〜2026-04-10を1回実測し、8行・433msだった。現行の1銘柄×1日方式は4,448銘柄を4件/分で約18.5時間が目安だが、期間方式の全期間ページング回数と429率は追加実測が必要。
- 完了: DB非破壊の期間測定ジョブを追加し、86970の10日間（8行・129ms）とFree plan範囲約2年（481行・575ms・HTTP 200）を実測。最新取得可能日優先、履歴古い順、ページング、429の`retry_pending`、5分クールダウンを実装し、全銘柄の展開はレート制限を守って継続する。

  2026-07-25追補: Raspberry Piで銘柄マスター更新だけが繰り返される状態を確認したため、常駐コレクターがマスター更新の成功を`JobRun`へ記録するよう修正した。最新候補日の`unavailable`は6時間ごとの再確認に制限し、待機中は履歴日を古い順に補完する。MacのDocker内pytestは全件成功し、DBバックアップ後にPiへ配置・再起動した。反映後は`phase=price_collection`、2024-05-07の取得成功、価格件数の増加を確認した。

- [x] **P1-3 本番マイグレーションとバックアップを安全化する**
  - Compose起動時の複数サービスによる自動マイグレーションを廃止し、バックアップ後の単一明示操作へ分離する。
  - バックアップ失敗をJobRunへ記録し、リストア試験を追加する。
  - 完了条件: PostgreSQL統合テスト、復元手順、Raspberry Piでの承認済み確認。

- 完了: 明示マイグレーション、PGPASSFILE、原子的バックアップ、失敗JobRun、Mac/Pi一時DB復元試験、PostgreSQL 16クライアント統一を完了。

## P2: 波及分析の統計品質

- [x] **P2-1 条件別統計の仕様を完全化する**
  - 境界値を仕様どおりに分類する。
  - 方向一致率、継続・反転率、最大利益・損失、信頼区間を追加する。
  - 完了: 境界を含む固定ビン、標本数、方向一致率、平均・中央値、最大／最小値、平均の95%近似信頼区間を返す。空・少数標本は欠測として扱う。

- [x] **P2-2 回帰・Granger検定を再定義する**
  - セッション時点とラグの意味を明示し、Grangerを因果の証明として扱わない。
  - HAC等の推定方針、多重比較の警告、データ品質ゲートを決める。
  - 完了: ラグ規則を固定し、OLSはHAC（最大ラグ3）、Grangerは予測上の先行性に限定し、検定ラグ数によるBonferroni補正値を保存・表示する。30件未満は推定しない。

- [x] **P2-3 ウォークフォワード検証を実装する**
  - 学習・検証期間を分離し、先読みのない結果だけを表示する。
  - 入力データ版、ルール版、価格基準、source、実行時刻を保存する。
  - 完了: 予測対象行を学習に含めない逐次ウォークフォワードを追加し、回帰結果へHAC設定、入力来歴、実行時刻を保存する。Streamlitにも検証結果を表示する。

## P3: 表示・販売表現

- [x] **P3-1 現在地を明示する**
  - 通常／デモ、source、遅延、最終データ日、価格基準、対象期間、標本数、欠測警告を画面に表示する。
  - ウォークフォワード検証完了までは、予測・推奨・販売可能と誤認される表現を使わない。
  - 完了: Streamlit共通領域に通常／デモ、品質警告数、最終データ日、最終取得時刻、source方針、価格基準、対象期間を表示し、各分析タブの不足データ警告・分析時点・投資助言ではない旨を確認した。システム管理タブには取得ログ、ジョブ履歴、旧結果の再計算要否を表示する。

- [x] **P2-4 業種・銘柄感応度を追加する**
  - 完了: J-Quants銘柄マスターの業種メタデータと観測済み日米波及データから、業種別・銘柄別の標本数、銘柄数、平均リターン、相関、傾きを決定論的に計算し、波及タブへ表示する。少数標本は除外し、予測・推奨とは表示しない。

## 再開時の順序

1. フェーズ3の財務・ETF固有指標のデータソース設計
2. フェーズ3の銘柄・ETF分析実装
3. Gemini導入可否の確認

## 2026-07-13 終了時点の引き継ぎ

- 30営業日以上の調整済み価格履歴が蓄積されるまで、フェーズ3の追加実装は拡大せず、データ収集と品質確認を優先する。
- `jobs/check_operations.py` が `adjusted_history_ready_symbols` を出力する。2026-07-13確認時点でMac・Raspberry Piともに `0`。
- Raspberry Pi確認値: 調整済み価格2,301件、対象銘柄2,239、銘柄別最大2日・中央値1日。常駐コレクターは稼働継続中で、429は`retry_pending`として再試行対象に保持する。
- 30営業日到達の見込みは、1営業日分の全銘柄取得が約18〜19時間、30営業日分が約22〜24日となるため、429や通信再試行を含め約3〜4週間。最初の到達銘柄は先行する可能性があるが、全体の到達は数日から1週間程度遅れる見込み。
- Streamlitの「銘柄・ETF分析」に、取得済み財務サマリーからPER・PBR・ROE・営業利益率を表示する指標カードを追加し、Raspberry Piへ反映済み。未取得値は`-`で表示し推測しない。
- Macでpytest 84件成功、運用確認ジョブ成功、Raspberry PiのStreamlitヘルスチェック`ok`。コミットは `250935a Show financial ratios and history readiness`。

### 次回再開時に最初に行うタスク

1. Raspberry Piで`adjusted_history_ready_symbols`、調整済み履歴件数、429・`retry_pending`件数、ディスク容量を読み取り確認する。
2. 30営業日以上の銘柄が出た場合、Mac側の「変動候補」タブを開いて候補表示と品質警告を確認するよう依頼する。
3. 30営業日未達の場合は、収集速度と失敗率を再計算し、期間取得方式の改善余地を評価する。
4. 30営業日到達後に、フェーズ3の銘柄・ETF分析の残タスク（ETF指標の実データ取り込み、スクリーニング品質確認）へ進む。

### 2026-07-14 追加引き継ぎ

- Raspberry Piの直近確認では、`market_prices`は約67,000件、調整済み履歴の最大は4日、30営業日以上は0銘柄。財務スナップショットは13件、ETF指標スナップショットとSEC CIK紐付けは0件。
- 進捗確認ジョブは調整済み履歴を1・5・10・20・30営業日閾値別に記録し、価格基準別件数、財務・ETFスナップショット、SEC CIK件数も運用履歴へ表示する。
- Mac側の財務データ表示は、スクリーニング上位200銘柄外でも保存済み財務銘柄を選択可能。ImportError修正後、Mac・Raspberry PiのStreamlitヘルスチェックは`ok`。

### 2026-07-14 作業中止時点の再開情報

- ブラウザで発生した`ImportError: cannot import name 'list_fundamental_symbols'`は、画面コードとサービスコードの反映ずれが原因。MacのDockerイメージを再ビルドし、ラズパイにもサービスコードを反映して解消した。
- 現在のブラウザ表示で「実データがありません」と出る場合は、ImportErrorではなくMac側にFRED指数データが未取得であることによる品質警告。J-Quantsの日本株・ETFは「銘柄・ETF分析」、収集状況は「システム管理」で確認する。
- Mac・Raspberry PiともStreamlitヘルスチェック`ok`。Docker内pytestは96件成功。財務指標のゼロ／NaN分母、UTC開示日時、SEC開示日混在防止の品質テストを追加済み。
- サブエージェントによるロードマップ確認では、P0〜P3是正は完了。30営業日未達の間は収集監視、品質確認、財務・ETF表示、SEC明示実行基盤を進め、変動候補・仮想評価・本格バックテストは30営業日到達後とする合意。
- 直近コミット: `8852d42`（財務・SEC品質テスト）、`e33f515`（運用引き継ぎ更新）。

### 次回再開時に最初に提示する作業

1. ラズパイの最新収集状況（価格件数、閾値別調整済み履歴、429、容量、財務・ETF件数）を読み取り確認する。
2. Macの「銘柄・ETF分析」で保存済み13件の財務スナップショットを表示確認する。
3. ETF指標の実データ投入方法を、無料条件・取得元・NULL扱いを確認したうえで決定する。
4. 30営業日到達まで、収集速度と品質警告を継続監視する。
5. 30営業日到達後に、変動候補・スクリーニング品質確認へ進み、その後フェーズ4のバックテストへ移る。

### 2026-07-20 再開作業の結果

- ラズパイの最新バックアップをMacへ取得し、Mac側の事前バックアップも保存した。
- Mac側画面確認用DBにラズパイ由来の財務スナップショット13件を復元し、`86970`の13件を確認。ETF指標は0件。
- Mac側のJ-Quants常駐収集は停止し、画面確認用DBを固定。ラズパイ側の常駐収集は継続している。
- Mac画面: `http://localhost:8501`、ヘルスチェック`ok`。
- 次の確認は「銘柄・ETF分析」で`86970`を選び、財務サマリーとPER等の表示を確認すること。ETF指標は未取得表示を確認する。

### 2026-07-20 検証用フェーズ4仮実装

- 30営業日未達の実データを無理に分析せず、`MARKET_DATA_MODE=demo` と明示した場合だけ合成データを使う候補・仮想評価の確認経路を追加した。
- 合成データは`source=sample`または`source=demo`として識別し、通常モードのJ-Quants/FRED経路から除外する。通常起動時の自動投入は行わない。
- `jobs/run_backtest.py --demo` はDBへ保存せず、短期・中期それぞれ初期250万円の仮想口座をメモリ上でシミュレーションする。手数料、売買単位、現金、実現損益を出力し、投資判断には使用しない警告を表示する。
- Streamlitの仮想投資評価タブにデモ警告と、通常モードでは30営業日以上の有効な調整済み履歴が必要である旨を表示する。
- Docker内pytestは99件成功。デモジョブの出力確認も完了。ラズパイへの反映は不要（デモ専用であり、常駐収集へ影響させない）。

### 次に提示する作業

1. Macでデモモードを明示起動し、候補・仮想投資評価タブの表示を画面確認する。
2. 実データはラズパイで収集を継続し、30営業日到達状況を読み取り確認する。
3. 実データ到達後、デモ経路と分離したフェーズ4本実装（判断カード、約定仮定、バックテスト品質ゲート）へ進む。

### 2026-08-15 フェーズ3品質ゲート再開

- Raspberry Piの実データをMacへ非破壊で複製し、復元前のMac DBもバックアップした。
- 短期スクリーニングの開始条件を、候補抽出・仮想評価と同じ「重複しない有効な調整済み価格30営業日以上」に統一した。
- 50日・75日移動平均はスクリーニング全体の開始条件ではなく、各指標固有の必要観測数として扱う。
- 通常モードでは`raw_ohlcv_with_adjusted`だけを対象とし、DB側で品質ゲート通過銘柄を選んでから最大200銘柄を画面へ読み込む。
- 画面に観測数、データ時点、価格基準を表示し、未取得値は推測しない。
- 決定論的な注目度、根拠、品質警告、対象区分・業種・最低注目度フィルターを追加した。注目度は値動きの偏りを示す比較値に限定し、上昇確率や売買推奨として扱わない。
- J-Quants Free planの約12週間遅延をスクリーニング画面内に明示した。
- 実データ確認で2024年の補完履歴と2026年の最新付近を単純な30件として数え、20日騰落率が年単位の空白をまたぐ問題を検出した。東証カレンダー上の最新連続セッションだけを候補・スクリーニングに使用し、連続30営業日未満は分析対象外にするよう是正した。
- 運用確認の`adjusted_history_ready_symbols`も最新連続セッション基準へ変更し、従来の単純観測件数は`adjusted_observed_history_*`として別に残した。
- 2026-08-15のMac復元DBでは、単純観測件数30件以上は約3,000銘柄ある一方、最新の連続セッションは最大11営業日、連続30営業日以上は0銘柄だった。過去に報告した「30営業日以上」は単純件数であり、分析可能な連続履歴数ではなかったため訂正する。
- この確認時点の収集順は「最新取得可能日を全銘柄→古い日付を古い順」であり、最新付近の途中欠損を優先補完していなかった。分析開始を早める「最新日→直近30営業日の欠損→古い履歴」の三段階キューは、下記のユーザー承認後に実装・配置した。
- 変動候補画面の「判定可能銘柄数」も単純件数ではなく最新連続セッション基準へ修正し、仮想評価も空白期間をまたぐ価格列から取引を生成しないよう統一した。
- Docker内の全回帰テストは107件成功。Mac版Streamlitで、スクリーニングの連続30営業日警告と、変動候補の判定可能銘柄数0件を確認した。ラズパイの収集順と配置状態は変更していない。

### 2026-08-15 直近連続履歴を優先する収集キュー

- ユーザー承認により、J-Quantsの収集順を「最新取引日を全銘柄→直近30取引日の欠損を新しい日付から→残りの約2年を古い日付から」へ変更する。
- 最新候補日が土日・休場日の場合は、東証カレンダー上の直前取引日を最新フェーズとして扱う。
- `queue_phase`を`latest`、`recent_gap`、`history`としてジョブ履歴へ記録し、実際にどの段階を処理しているか確認可能にする。
- 既存の価格、対象日・銘柄別進捗、`no_data`、`retry_pending`は保持し、再起動後も未取得分から再開する。API間隔と再試行方針は変更しない。
- Macの関連テスト12件と全回帰テスト110件が成功した。コミットは`2072005`。
- ラズパイ配置前バックアップは`market_signal_lab_20260815_045321.dump`。収集ジョブ、Compose設定、関連テスト・文書だけを限定配置し、DBマイグレーションは行っていない。
- ラズパイ上の関連テスト12件が成功し、collectorだけを再作成した。ログで`latest:2026-05-15`から`recent_gap:2026-05-14`、さらに`2026-05-13`へ移行したことを確認した。
- 配置後はapp・db・collectorが稼働、DBヘルスチェック正常、`restart: unless-stopped`を維持。`market_prices=185,744`、最新日4,226件、2026-05-14は4,223件、ディスク使用率12%（96GB空き）。
- Macの「システム管理」に、収集フェーズ、対象日、連続30営業日到達数、直近30営業日のカバー率・残り要求上限・理論最短時間を追加した。常駐collectorの大量ジョブに埋もれないよう、`check_operations`をジョブ名で独立取得する。
- Mac復元DBの実行確認では、直近30営業日カバー率54.7%、残り要求上限60,705件、理論最短252.9時間、連続最大11営業日、到達0銘柄。localhostはMacスナップショットであり、ラズパイのリアルタイム値ではないことを画面へ明記した。
- 全タブの通常読み込み時に、非表示の日米波及タブが全銘柄感応度を自動計算してCPUを占有していた。感応度計算を明示切替に変更し、運用画面の長時間ロードを解消した。
- J-Quants APIのレート制限競合を避けるため、ラズパイ収集中はMacの`jquants-collector`を停止し、Macの標準起動を`db app`だけに変更した。

### 2026-08-15 ラズパイ運用表示の配置確認

- 配置前に`market_signal_lab_20260815_051900.dump`を作成し、監視表示とその依存コード・テストだけを限定配置した。DBマイグレーションはない。
- ラズパイ内の関連pytestは21件成功。appだけを再起動し、dbと`jquants-collector`は停止せず、配置後も収集のHTTP 200成功を確認した。
- `jobs/check_operations.py`の実測は、`market_prices=185,780`、収集段階`recent_gap`、対象日`2026-04-24`、直近30営業日カバー率54.7%、完了日11/30、残り要求上限60,627件、理論最短252.6時間、最新連続履歴最大12営業日、30営業日到達0銘柄。過去24時間の失敗・再試行ジョブは0件だった。
- ディスク使用率は11.52%で空き約102.8GB、メモリ使用率は約15.3%。StreamlitのHTTPヘルスチェックは`ok`。
- ラズパイの実画面をブラウザで開き、「システム管理」に上記の収集段階、対象日、到達数、カバー率、残件、理論最短時間が表示されることを確認した。
- 次の品質ゲートは最新連続30営業日である。到達まではcollectorを継続し、節目で`check_operations`を再実行する。到達後にラズパイDBをMacへ非破壊で複製し、スクリーニング、変動候補、仮想評価の実データ確認へ進む。

### 2026-08-15 Mac画面の遅延読み込みとフェーズ4デモ口座

- Streamlitの`st.tabs`を選択式ナビゲーションへ変更し、選択した画面だけが分析・DB取得を実行するようにした。共通の品質表示は相関等を計算しない軽量経路へ分離した。
- デモモードでは「仮想投資評価」を初期表示し、DBへ接続せず合成価格・合成ニュースだけで短期・中期各250万円の独立口座を実行できる。
- 暫定約定仮定は、判断日の次の東証営業日終値、手数料0.10%、スプレッド0.10%、税率0%、100株単位、同時保有2銘柄、1銘柄30%上限。利益確定、損切り、保有期限、条件悪化を決済理由として分離した。
- Docker内の全pytestは122件成功。MacのDocker版で初回デモ表示約4.0秒、デモ口座計算約1.2秒、別画面への切替完了約1.9秒をブラウザ確認した。HTTPヘルスチェックは`ok`。
- DB・マイグレーション変更はなく、ラズパイには未配置。実データ経路の最終確認は最新連続30営業日到達後に行う。

### 2026-08-15 実投資支援前の仮想取引P0是正

- `docs/19_investment_decision_readiness_assessment.md`の評価に基づき、実データ用仮想取引の同日終値エントリーを廃止した。シグナル日の次の東証営業日の調整済み始値でエントリーし、始値欠損時は取引を生成しない。
- 米国市場コンテキストもシグナル日より前の価格だけに制限し、同日の未確定米国終値を使用しない。
- 下方向シグナルを空売り利益に変換する処理を停止した。下方向と方向未確定は`observe_only`とし、買いのみ仮想口座から除外する。
- 仮想取引3件から候補スコアを増減する処理を停止した。銘柄別成績は「スコア未反映」の未較正参考統計としてのみ表示する。
- `app/backtest/portfolio.py`に、現金をエントリー時に拘束するイベント順の買いのみ口座を追加した。同時保有上限、1銘柄上限、100株単位、手数料、スプレッド、設定可能な税率を扱い、同日引け決済代金を同日寄付きへ遡って再利用しない。
- 総損益、最大ドローダウン、完了取引数、勝率、平均取引損益、日経平均の同期間騰落率と超過差を計算する。ベンチマークのシグナル日からエントリー日前までの変動は比較へ含めない。
- MacのDocker内で全130件のpytestが成功した。DB・マイグレーション変更はなく、ラズパイには未配置。
- 未完了: デモ口座と共通口座の完全統一、流動性、値幅制限、部分約定、配当、企業行動、為替、空売り、長期時点整合データ、アウト・オブ・サンプル検証、前向き仮想運用。

### 2026-08-15 フェーズ4共通エンジンと検証基盤

- デモ口座と実データ評価を`app/backtest/ohlc.py`の同じイベントエンジンへ統一した。判断後の次セッション始値でのみ買い、日次OHLCで利益確定・損切り・保有期限を判定する。同一日足で利益確定と損切りが同時到達した場合は損切り優先、窓開け時は始値約定とする。
- エントリー日の完成出来高を先読みせず、前営業日出来高だけで参加率上限と部分約定を決める。取引停止、ストップ高の買い、ストップ安の売りは、入力データにフラグがある場合に約定させない。
- 業種集中上限、口座ドローダウン停止、連続損失後のクールダウンを追加した。下方向は引き続き観察専用で、空売り利益を計上しない。
- 1取引と全保有の許容損失率を明示し、損切り幅から参考数量と計画損失額を逆算して判断カードへ残す。
- 戦略版、約定版、入力データ版、シグナル・価格ハッシュから決定論的な監査IDを作り、根拠、反対材料、価格水準、無効条件、人の確認要否を判断カードへ保持する。
- 固定ルールを学習期間後の未見期間だけへ渡すウォークフォワード基盤を追加した。デモ画面で期間別結果を確認できるが、合成データの結果は有効性評価に使わない。
- `jobs/run_forward_shadow.py`と画面ボタンで、DBや実注文へ書き込まず、Git対象外の`data/forward_shadow/`へ不変JSONを保存できる。ラズパイへの定期配置は行っていない。
- MacのDocker内で全151件のpytest、デモバックテスト台帳出力、デモ前向き観察JSON、Streamlit画面とHTTPヘルスチェックを確認した。
- 未完了: 長期の時点整合実データによるアウト・オブ・サンプル評価、6〜12か月程度の前向き観察蓄積、配当・企業行動・為替・上場廃止、実際の値幅制限・特別気配データ、動的スプレッド、ニュース公表時刻の完全な時点整合。

### 2026-08-16 フェーズ4の約定・検証・監査強化

- 前営業日売買代金5千万円を最低基準とし、10億円以上・2.5億円以上・5千万円以上の3帯で保守的な代理スプレッド／基礎スリッページを適用する。銘柄別の明示値がある場合は版付き上書きを優先する。
- 特別気配フラグが入力にある場合、エントリーと決済を約定不能として扱う。取得元に実フラグがない場合は推測しない。
- 判断日時点までの終値だけで保有銘柄との相関集中を確認し、0.85以上は見送る。業種・計画損失・ドローダウン・連続損失制御も維持する。
- 判断カードを上書き型からイベント時系列型へ変更し、同じカードIDのエントリー、決済見送り、決済を別event IDで保存する。
- 複数ベンチマーク比較と、異なるルールによる検証期間の重複利用を拒否する不変レジストリを追加した。レジストリへの書き込みは明示実行に限る。
- 未完了: 長期の時点整合実データ評価、6〜12か月の前向き観察、配当・企業行動・為替・上場廃止、実際の板・特別気配・値幅制限データ、ニュース公表時刻の完全な時点整合。

### 2026-08-16 未見期間登録と日次前向き観察ジョブ

- ウォークフォワードの各未見期間を、短期・中期の評価系列ごとにシミュレーター実行前へ登録する経路を追加した。変更後ルールによる同一系列・重複期間の再利用は拒否し、同一登録は冪等とする。
- 前向き観察JSONへ取引、見送り、ベンチマーク比較、口座状態を追加した。`--daily`ではJSTの東証営業日ごとに最初の結果を固定し、同日の異なる入力による上書きを拒否する。候補なし・品質ゲート未達の日も状態として記録する。
- JST日付をUTC前日へずらして営業日判定していた不具合を修正し、東証カレンダーの現地暦日を保持するテストを追加した。
- Macの平日18:30用`launchd`テンプレートを追加した。テンプレートは未登録で、Macへの登録・常時実行はユーザー承認後に行う。ラズパイへの配置、DB変更、自動発注はない。
