# AGENTS.md — Market Signal Lab

## 1. この文書の目的
本リポジトリで作業するCodexおよび開発エージェント向けの最上位指示です。
実装前に本ファイルと `docs/` 配下の仕様書を読み、既存コード・設定・テストを確認してください。

## 2. プロダクト概要
Market Signal Labは、日本株、米国株、日本ETF、米国ETFを対象とする投資判断支援システムです。
市場環境、銘柄選定、エントリー、利益確定、損切り、ポジションリスクを分析し、利用者が根拠を確認した上で最終判断を行えるよう支援します。

自動売買は実装しません。証券会社への注文は利用者が手動で行います。

## 3. 役割分担
- Codex: 設計、実装、テスト、ドキュメント、Docker、デプロイ支援
- Raspberry Pi: データ収集、DB、数値分析、定期処理、Gemini API、Slack Bot、Streamlit
- Python: 指標計算、統計分析、スコア、バックテスト、価格水準計算
- Gemini: 計算済みデータの解釈、複数観点の統合、説明、対話
- Slack: 利用者とGeminiエージェントの主要な対話窓口
- 利用者: 最終的な投資判断と手動注文

## 4. 開発原則
1. 一度に全機能を実装しない。`docs/10_development_roadmap.md` の順序に従う。
2. 各フェーズ終了時にアプリを起動可能な状態に保つ。
3. 既存ファイルを変更する前に内容を確認し、不要な全面書き換えを避ける。
4. 数値計算をLLMへ委ねない。GeminiへはPythonで計算・検証済みの構造化データを渡す。
5. 生の大量時系列をGeminiへ送らない。
6. 将来データの混入、先読みバイアス、サバイバーシップバイアスを避ける。
7. 米国市場と日本市場を同じ日付で単純結合しない。取引セッション、休日、夏時間、情報利用可能時点を考慮する。
8. 欠損値を安易に前方補完しない。
9. APIキー、Slackトークン、DBパスワードは `.env` で管理し、Gitへ登録しない。
10. ARM64対応を維持し、Apple Silicon MacとRaspberry Pi OS 64-bitの双方で動作させる。
11. 外部APIの無料枠、利用規約、レート制限を尊重する。
12. 取得元、取得時刻、データ鮮度、欠損・品質警告を保存・表示する。
13. 分析ルール、モデル、プロンプトにバージョンを付け、再現可能にする。
14. 投資助言や利益保証と誤認される断定表現を避ける。

## 5. 推奨技術
- Python 3.12
- Streamlit
- PostgreSQL
- SQLAlchemy 2.x
- Alembic
- pandas / numpy
- Plotly
- statsmodels
- scikit-learn（必要時）
- httpx / tenacity
- pydantic-settings
- pytest
- Slack Bolt for Python / Socket Mode
- Gemini API
- Docker / Docker Compose

## 6. 実装前の必須手順
1. `pwd`、`git status`、ディレクトリ構成を確認する。
2. 本ファイルと `docs/` 配下を読む。
3. 既存コード、設定、マイグレーション、テストを確認する。
4. 現状と仕様の差分、矛盾、リスクを簡潔に報告する。
5. 対象フェーズの実装計画と完了条件を示す。
6. 実装、テスト、README更新を行う。
7. 実行したコマンド、変更ファイル、テスト結果、残課題を報告する。

## 7. 初回実装の範囲
初回はフェーズ1のみを実装します。
- プロジェクト骨格
- Docker Compose
- PostgreSQL
- SQLAlchemy
- Alembic
- 資産マスター
- 日次OHLCV価格テーブル
- データプロバイダー抽象化
- サンプルまたは利用可能な無料データ取得
- 日次リターン
- 米国市場セッションと次の日本営業日の対応
- 基本相関
- Streamlit基本画面
- pytest
- MacおよびRaspberry PiのREADME

Gemini、Slack、売買判断、利益確定・損切り判断は初回に実装しません。

## 8. 完了報告の形式
- 実装概要
- 変更ファイル
- DB変更
- 実行方法
- テスト結果
- 既知の制約
- 次フェーズ候補
