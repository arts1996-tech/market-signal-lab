# Market Signal Lab Agent Guide

## 1. この文書の目的と優先順位

本書は、このリポジトリで作業するCodexおよび開発エージェント向けの最上位指示です。実装・変更・レビューの前に必ず読み、既存コード、設定、マイグレーション、テスト、Gitの状態を確認してください。

要件の優先順位は次のとおりです。

1. 本書の製品要件、アーキテクチャ方針、投資分析ルール、セキュリティ方針、開発ロードマップ
2. `docs/01_product_vision.md` から `docs/16_slack_free_plan_review.md` までの仕様・受け入れ基準・レビュー
3. `docs/17_remediation_todo.md` の未完了ToDoと完了条件
4. `README.md` の現行環境に対応した起動・運用手順
5. 既存コードとテストが示す現在の挙動

- `AGENTS.market-signal-lab.md` と `README.market-signal-lab.md` の新しい製品方針は本書へ統合済みとして扱い、要件を後退させない。
- レビュー文書やオプション提案は、それ自体が要件変更であると明記されていない限り、必須要件と区別する。
- 文書、コード、運用状態が矛盾する場合は勝手に削除・単純化せず、差分、影響、推奨案をユーザーへ報告してから変更する。
- 既に実装済みの機能は、ロードマップ上の順序を理由に削除または無効化しない。以後の変更は段階的に行い、各段階で起動可能な状態を保つ。
- 評価会議後の是正作業は `docs/17_remediation_todo.md` の優先度・依存順に従う。未完了のP0を迂回して分析機能やRaspberry Piへのフェーズ2配置を進めない。
- UI（`app/dashboard/`）は例外とする。既存画面との互換性を維持する必要はなく、製品要件に合わせて画面構成、導線、表示内容、コンポーネントを全面的に削除・置換・再設計してよい。
- UIを刷新しても、明示的な対象変更がない限り、データ、分析ロジック、DBマイグレーション、ジョブ、秘密情報管理の既存要件は維持する。

## 2. プロダクト概要

Market Signal Labは、日本株、米国株、日本ETF、米国ETFを対象とする投資判断支援システムです。市場環境、銘柄選定、エントリー、利益確定、損切り、ポジションリスクを分析し、根拠、反対材料、過去検証、データ品質を利用者が確認した上で最終判断できるよう支援します。

### 投資対象

- 日本株
- 米国株
- 日本ETF
- 米国ETF

### 参考データ

- 日経平均、TOPIX
- NASDAQ Composite、NASDAQ 100、S&P 500、Dow Jones
- SOX等のセクター指標、VIX
- USD/JPY
- 米国10年債利回り、日本の主要金利
- 必要に応じて金、原油等

為替は円建て評価と市場分析の参考データとして扱い、FX取引そのものは投資候補にしません。

### 想定投資期間

- 短期: 1〜20営業日を中心
- 中期: 1か月〜1年程度

### 非目標・禁止事項

- 自動売買、証券会社への発注、注文機能を実装しない。
- 利益保証や「必ず上がる」「買うべき」等の断定をしない。
- 分析結果を投資助言として表示しない。最終判断と手動注文は利用者が行う。
- LLMに未検証の価格・指標計算をさせない。
- データに存在しないOHLCVや事実を推測・生成しない。
- 初期対象に投資信託、暗号資産、FX取引、先物・オプションを加えない。
- 任意SQL、任意コード、許可されていない外部操作を利用者入力やLLMから実行させない。

## 3. 開発時の役割

常に次の3つの観点を持って作業します。

- 作業者 (Developer): 要求を満たす設計、実装、リファクタリング、バグ修正、ドキュメント、Docker支援を行う。
- 評価者 (Evaluator): 設計、可読性、セキュリティ、データ品質、運用、投資分析上のバイアスをレビューする。
- テスター (Tester): 正常系と異常系のテストを作成・実行し、回帰と受け入れ条件を確認する。

報告では必要に応じて `[作業者]` `[評価者]` `[テスター]` の短い観点を示し、内部の長い推論ではなく、実施内容と判断結果を簡潔に伝えます。

実行時の責務は次のように分離します。

- Raspberry Pi: データ収集、PostgreSQL、Python分析、定期処理、将来のGemini API、Slack Bot、Streamlitを運用する。
- Python: 指標、統計、スコア、価格水準、バックテストを決定論的に計算する。
- Gemini: Pythonで計算・検証済みの構造化データを解釈し、説明と複数観点の統合を行う。計算エンジンにしない。
- Streamlit: 数値、チャート、分析履歴、データ品質を確認する画面とする。
- Slack: フェーズ6以降の主要な対話・通知窓口とし、Streamlitを置き換えない。
- 利用者: 最終的な投資判断と手動注文を行う。

## 4. 開発原則

1. 一度に全機能を実装せず、`docs/10_development_roadmap.md` の順序を基本とする。
2. 各フェーズ、各Issueの終了時にアプリを起動可能な状態に保つ。
3. 既存ファイルと未コミット変更を確認し、ユーザーの変更を失わない。不要な全面書き換えを避ける。
4. 数値計算はPythonで行い、LLMには計算・検証済みの必要最小限の構造化データだけを渡す。生の大量時系列を送らない。
5. 将来データの混入、先読みバイアス、サバイバーシップバイアス、過学習を避ける。
6. 米国市場と日本市場を同じ日付で単純結合せず、取引セッション、休日、夏時間、情報利用可能時点を考慮する。
7. 欠損値を安易に前方補完しない。補完・除外・品質判定のルールを明示する。
8. 取得元、取得時刻、データ鮮度、欠損、改訂、品質警告を保存・表示する。
9. 分析ルール、モデル、プロンプト、入力データにバージョンを付け、再現可能にする。
10. API障害を局所化し、タイムアウト、再試行、レート制限、冪等性を実装する。
11. ARM64対応を維持し、Apple Silicon MacとRaspberry PiのDebian系64-bit OSで動作させる。x86専用依存を導入しない。
12. ダッシュボードに計算ロジックを置かず、外部API、DB、分析、サービス、UIを分離する。

## 5. 無料利用と外部サービス

- 原則として無料で利用できる技術、サービス、API、ライブラリを採用する。
- 有料サービス、有料API、有料プラン、無料枠の条件が不明なもの、将来的な課金リスクを判断できないものは、導入前に必ずユーザーへ確認する。
- 採用前に無料利用可否、無料枠、レート制限、利用規約、再配布条件、商用・個人利用条件、障害時の影響を確認する。
- 条件は変更され得るため、過去のレビューだけで確定せず、各フェーズ着手時に公式情報を再確認する。
- データソースの採用判断は `docs/data_sources.md` に記録し、プロバイダーを交換可能にする。
- スクレイピングは規約、安定性、保守性を確認し、安易に採用しない。
- Geminiはフェーズ5、Slackはフェーズ6の候補であり、料金・無料枠・規約・モデル選定を再確認し、ユーザー承認後に導入する。
- Gemini停止時はPython分析とStreamlitのみで縮退運転できるようにする。
- 複数LLMによるクロス検証はフェーズ5安定後のオプションであり、明示的な採用判断なしに実装しない。

### J-Quants

- J-Quants Free planは日本株・日本ETFの履歴分析候補として扱う。
- 現在の既存運用は株価が12週間遅延し、API制限を5件/分としている。実装・運用変更時には最新の公式条件を再確認する。
- 遅延データを最新の短期判断の根拠として扱わない。利用時は遅延とデータ時点を画面に明記する。
- 複数銘柄の連続取得は余裕を持った間隔と小さい `--limit` で検証してから増やす。
- 有料プラン、TOPIX四本値、指数四本値、分足、ティック、前場四本値、アドオンはユーザー確認なしに導入しない。

## 6. 技術スタックとアーキテクチャ

### 主要技術

- Python 3.12
- Streamlit
- PostgreSQL
- SQLAlchemy 2.x / Alembic
- pandas / numpy
- Plotly
- statsmodels
- scikit-learn（必要時のみ）
- httpx / tenacity
- pydantic-settings
- pytest
- Docker / Docker Compose
- Slack Bolt for Python / Socket Mode（フェーズ6以降、承認後）
- Gemini API（フェーズ5以降、承認後）

### データフロー

```text
External APIs
  -> Collectors / Providers
  -> PostgreSQL
  -> Python Analysis / Backtest
  -> Streamlit
  -> Gemini Agents（フェーズ5以降）
  -> Slack（フェーズ6以降）
```

### モジュール境界

- `app/collectors/`: 外部API固有クライアント
- `app/providers/`: 共通データ取得インターフェース。必要なフェーズで追加する
- `app/database/`: ORM、Repository、Alembic
- `app/analysis/`: 日次リターン、指標、相関、回帰、スコア等の決定論的計算
- `app/backtest/`: 時点整合的な検証。必要なフェーズで追加する
- `app/services/`: 収集、保存、分析を調停するユースケース
- `app/agents/`: Geminiプロンプト、ツール、構造化出力。フェーズ5で追加する
- `app/slack_bot/`: Slackコマンド、イベント、Block Kit。フェーズ6で追加する
- `app/dashboard/`: 表示のみ。入口は `streamlit_app.py`
- `app/core/`: 設定、ログ、例外、時刻
- `jobs/`: cronまたはsystemd timerから独立実行できるジョブ
- `tests/`: pytest
- `docs/`: 要件、設計、データソース、運用記録
- `docker/`: cron例などの運用補助

新しいモジュールは該当フェーズで追加し、構成を合わせるためだけの一括移動や既存 `services/` の削除は行いません。

## 7. データベースとデータ品質

- DB保存時刻はUTCとし、取引所タイムゾーンとセッション日を保持する。画面はJSTを基本に必要なら現地時刻を併記する。
- 同一価格データは少なくとも `asset_id + timeframe + price_time + source` の一意制約で重複登録を防ぐ。
- 価格には `open/high/low/close/adjusted_close/volume` を格納可能にするが、取得元に存在しない値はNULLとする。
- FRED由来の終値データに高値、安値、出来高がない場合、ローソク足、出来高、ATRを推測で表示しない。
- 取得処理は冪等にし、取得元、`fetched_at`、品質状態、取得ログ、ジョブ履歴を残す。
- 分析結果には分析種別、基準時刻、入力データ版、ルール版、モデル版、パラメータ、結果、作成時刻を保存する。
- `correlation_results` は後続分析で再利用可能にし、ペア別結果、米国指数群と日本指数群の平均リターン同士の相関、ペア別相関の平均サマリーを保存する。

## 8. 投資分析ルール

### 共通ルール

- 米国セッション終了後、次に開く日本営業日へ対応させる。日本休場中に複数の米国セッションがある場合は個別・累積双方を検討する。
- 分析時点で利用可能だった情報だけを使う。
- 相関は因果関係ではないことを画面に明記する。
- サンプル数、対象期間、信頼区間、モデル版、更新時刻、データ品質、不確実性を表示する。
- バックテストは学習・検証期間を分離し、ウォークフォワード検証を基本とする。
- 手数料、スプレッド、為替コストを設定可能にし、過学習と多重比較を警告する。
- Granger因果性は「予測上の先行性」として説明し、因果関係の証明として扱わない。

### 指標

- 日次・週次・月次リターン、SMA/EMA、RSI、MACD、ボリンジャーバンド、ボラティリティ、最大ドローダウンを扱う。
- OHLCVが利用可能な場合のみATR、ストキャスティクス、出来高比率、売買代金、ギャップ、支持・抵抗、ブレイクアウト等を計算する。
- 短期分析では既存の5/20/25/50/75日移動平均、EMA 12/26、RSI 14、MACD、ボリンジャーバンド、1/5/20日騰落率、簡易短期スコアを維持する。
- 中期では財務、バリュエーション、キャッシュフロー、業績予想修正、3/6/12か月モメンタム等を段階的に追加する。
- ETFでは経費、純資産、流動性、NAV乖離、トラッキングエラー、為替ヘッジ、レバレッジ区分等を取得可能な範囲で扱う。
- 米国資産はドル建てと円建てのリターン、為替寄与を併記する。

### 日米波及分析

- 米国前営業日と日本当日を対応させ、休日、連休、夏時間を考慮する。
- Pearson/Spearman相関、20/60/120/250日相関、60日ローリング相関、ラグ回帰、条件別集計、イベントスタディ等を段階的に実装する。
- 日本側はギャップ、場中、日次リターン等、取得可能な価格項目に対応する目的変数を使う。
- 出力は確率・想定レンジ・類似件数・感応度とし、断定的予測にしない。

### 判断と仮想評価

- 判断は「買い候補」「条件付き買い」「押し目待ち」「ブレイク待ち」「待機」「保有継続」「利益確定を検討」「損切り条件成立」「データ不足」等のシナリオ形式にする。
- 判断カードにはデータ時点、期間、エントリー条件、価格範囲、利益確定候補、損切り、無効条件、保有日数、リスクリワード、信頼度、根拠、反対材料、類似件数、品質警告、人の確認要否を含める。
- 変動候補は、日米相関、直近の米国指数変動、銘柄・ETFの短期指標、仮想投資評価のフィードバックを組み合わせ、根拠を表示する。
- 仮想投資評価は実注文を行わず、候補時点を再現した損益、エントリー理由、結果理由を表示する。
- 仮想評価は銘柄別に集計して後続スコアへフィードバックできるが、将来利益の保証に使用しない。
- フェーズ4では、短期用・中期用を分離した各250万円（JPY）の仮想口座を開始残高とし、エントリー、利益確定、損切り後の現金・評価額・実現／未実現損益・残高推移を追跡する。実装時に手数料、スプレッド、税金、約定仮定、為替コスト、売買単位、保有上限を明示し、実注文や口座間の資金移動は行わない。
- ポジションサイズは許容損失額とエントリー・損切り価格差から参考値を計算し、売買単位、通貨、保有上限、集中リスクを考慮する。最終投資額は利用者が決める。

## 9. GeminiとSlackのガードレール

### Gemini

- Pythonで計算済みの構造化データのみを解釈させ、データにない価格や事実を作らせない。
- 根拠と反対材料を併記し、少数サンプルや古いデータでは信頼度を下げる。
- 決算・イベント前、データ不足、品質低下を警告する。
- JSON Schemaによる構造化出力を検証し、不正な出力をDBやツールへ直接渡さない。
- 入出力JSON、モデル名、プロンプト版、トークン、実行時間、エラー、作成時刻を監査可能にする。

### Slack

- Socket Modeを使い、Raspberry PiをSlack連携のために外部公開しない。
- 許可ユーザーとワークスペースを制限し、銘柄、期間、コマンド、LLMのツール引数を再検証する。
- プロンプトインジェクション、指示上書き、許可外文字列、任意コード・SQL実行を防ぐテストを行う。
- 通知はレベル、重複排除、クールダウンを持つ。
- Slackの履歴やファイル容量を永続記録の代わりにせず、必要な監査情報はPostgreSQLへ保存する。
- 会話履歴を毎回すべてLLMへ送らず、現在のスレッド、対象の直近分析、必要な保有情報、要約だけを使う。

## 10. セキュリティ方針

- APIキー、Slackトークン、DBパスワード等は `.env` で管理し、Gitに含めない。
- `.env.example` は秘密値を空にし、ログ、例外、テスト結果、バックアップへ秘密情報を出さない。必要に応じてマスキングを実装・テストする。
- StreamlitとPostgreSQLを無制限にインターネット公開しない。PostgreSQLは内部ネットワークまたはlocalhostに限定する。
- 外部アクセスが必要な場合はTailscale等を候補とし、料金・規約・構成を確認してユーザー承認後に導入する。
- SQLAlchemyの安全なクエリを使い、銘柄コード、期間、数値パラメータを境界で検証する。
- 外部APIの入力と出力を信頼せず、型、範囲、鮮度、欠損を検証する。
- 本番Raspberry Piへのデプロイ、DB変更、復元、cron変更など運用状態を変える操作は、対象と影響を示してユーザーの明示的な依頼または承認を得てから行う。
- 本番マイグレーションは事前バックアップとMac側での検証を行い、破壊的変更を自動適用しない。

## 11. 段階的開発ロードマップ

ロードマップは今後の依存順序を示します。既存実装を後退させる指示ではありません。各フェーズの詳細と完了条件は `docs/10_development_roadmap.md` と `docs/12_acceptance_criteria.md` を参照します。

1. 基盤・市場比較: Docker Compose、PostgreSQL、ORM、Alembic、資産、日次価格、プロバイダー、リターン、日米対応、相関、Streamlit、pytest
2. 日米波及分析: ギャップ・場中、条件別集計、回帰、Granger検定、感応度、ウォークフォワード検証
3. 銘柄・ETF分析: 日米株・ETF、テクニカル、財務、ETF固有指標、円建て評価、スクリーニング
4. 売買計画・バックテスト: エントリー、利益確定、損切り、無効条件、参考ポジションサイズ、判断カード
5. Gemini: 承認後にAPI、構造化出力、専門・統括エージェント、監査、Pythonフォールバック
6. Slack: 承認後にSocket Mode、コマンド、DM、スレッド、Block Kit、定期レポート、認可
7. 保有管理・通知: ポートフォリオ、再評価、アラート、重複排除
8. Raspberry Pi本番化: 既存配置を基礎に、SSD、リソース測定、監視、自動復旧、バックアップ・リストアを完成させる

対象フェーズ外の大規模な先行実装を行いません。既存の先行実装を変更する場合も、現在の動作とテストを維持します。

## 12. 実装前後の必須手順

### 実装前

1. `pwd`、`git status`、ディレクトリ構成を確認する。
2. 本書、対象フェーズの `docs/`、`README.md` を読む。
3. 対象コード、設定、マイグレーション、テスト、関連ジョブを確認する。
4. 現状と仕様の差分、矛盾、無料利用条件、セキュリティ・運用リスクを整理する。
5. 変更範囲、完了条件、テスト方法を示す。

### 実装後

1. 関連テストと必要な回帰テストを実行する。
2. 起動方法、設定、DB、運用が変わった場合はREADMEと関連文書を更新する。
3. 変更ファイル、DB変更、実行方法、テスト結果、既知の制約、次フェーズ候補を報告する。
4. APIキー、`.env`、ログ、バックアップ、DBデータがGit対象に入っていないことを確認する。

## 13. テスト方針

- 通常の開発中確認はMacで行う。Pythonテスト、Alembicの検証、Docker Compose起動、ブラウザによるUI確認を、変更の粒度に応じて実施する。
- ラズパイは開発中の常時確認先ではなく、フェーズ完了、DBマイグレーション、Docker・cron・バックアップなど運用に関わる節目で行う統合・実運用確認先とする。
- Macで確認可能な仕組み（サンプルデータ、ローカルDB／Compose、pytest、READMEの手順）を維持・改善し、ラズパイへ依存しなければ確認できない開発フローにしない。
- ラズパイへの配置、再ビルド、マイグレーション適用、ジョブ実行など状態を変える確認は、ユーザーの明示的な依頼または承認後にだけ行う。
- 通常はDockerで実行する。

```bash
docker compose run --rm app pytest
```

- Dockerが利用できない場合はPython 3.12環境を使う。

```bash
python3.12 -m venv .venv312
.venv312/bin/python -m pip install -e ".[dev]"
.venv312/bin/python -m pytest
```

- 少なくとも変更対象に応じて、リターン、RSI、MACD、ATR、相関、ローリング相関、回帰、日米セッション対応、休日、連休、夏時間、重複登録、API再試行、先読み防止、円建てリターンを確認する。
- Gemini／Slack導入後は構造化出力、障害時フォールバック、コマンド解析、認可、Block Kit、通知重複排除、プロンプトインジェクションも確認する。
- コミット前にpytestを実行する。DockerやDBに関わる変更は、可能なら `docker compose up --build` も確認する。

## 14. よく使うコマンド

```bash
cp .env.example .env
docker compose up --build
docker compose exec app python jobs/collect_us_market.py
docker compose exec app python jobs/collect_jquants_listed_info.py --limit 20
docker compose exec app python jobs/collect_jquants_daily.py --code 86970 --date 20260401 --name "JPX" --asset-type stock
docker compose exec app python jobs/collect_jquants_daily_batch.py --date 20260401 --limit 3
docker compose exec app python jobs/collect_jquants_recent_daily_batch.py --limit 3 --lag-days 91 --lookback-days 5
docker compose exec app python jobs/run_short_term_analysis.py
docker compose exec app python jobs/run_spillover_analysis.py --jp-symbol 13060
docker compose exec app python jobs/run_mid_term_analysis.py
docker compose exec app python jobs/run_backtest.py
MARKET_DATA_MODE=demo docker compose run --rm app python jobs/seed_sample_data.py --demo
docker compose exec app python jobs/backup_database.py
docker compose run --rm app pytest
```

## 15. GitHub運用

- リポジトリ: `https://github.com/arts1996-tech/market-signal-lab`
- 作業は小さなIssue単位に分け、1 Issueを1目的に絞る。
- 分類は `feature`、`bug`、`data`、`analysis`、`ops`、`docs` を基本とする。
- PRには目的、変更内容、確認結果、DB変更、既知の制約、無料利用方針への影響を記載する。
- ユーザーの未コミット変更を上書き、破棄、巻き戻ししない。
- APIキー、`.env`、ログ、バックアップ、DBデータをコミットしない。

## 16. Raspberry Pi運用と現在地

### 現在の実運用環境（2026-07-11時点）

- 接続先: ローカルLAN内の `raspberrypi.local`。`192.168.100.127` は2026-07-11確認時点のIPであり、DHCP等により変わり得る。
- SSHユーザー: `tsurusumu`
- Mac側SSH鍵: `~/.ssh/travel_price_monitor_rpi`
- 通常は `raspberrypi.local` を使う。名前解決やホスト鍵登録が未設定の場合は、現在のIPを確認してから接続する。
- 接続例: `ssh -i ~/.ssh/travel_price_monitor_rpi tsurusumu@raspberrypi.local`
- SSHホスト鍵（ED25519）指紋: `SHA256:TZq+z1REo3s9bBcUcVGvhfDb5Hj6pqrH/vtuo37Ws6I`。IP変更後もこの指紋を照合し、変更を検知した場合は接続せず利用者に確認する。
- 秘密鍵の実体、パスフレーズ、`.env` の値はプロジェクト内へ保存しない。
- OS: Debian GNU/Linux 13 (trixie)、`aarch64`
- Docker: `26.1.5+dfsg1`、Docker Compose: `2.26.1-4`
- 配置先: `/home/tsurusumu/market-signal-lab`
- `.env` は配置済み。内容はGitに含めない。
- `docker compose up -d --build` 実行済み。
- アプリ: `http://192.168.100.127:8501` でHTTP 200確認済み。
- PostgreSQLのホスト公開: `127.0.0.1:5432:5432` に限定済み。
- cron: `docker/raspberry-pi.crontab` を登録済み。
- cronログ: `/home/tsurusumu/market-signal-lab/ops_logs/`

### 運用要件

- `restart: unless-stopped`、ヘルスチェック、構造化ログ、ログローテーションを維持する。
- ジョブの冪等性、失敗履歴、ディスク・DB容量、データ鮮度を監視する。
- バックアップは `pg_dump` で日次保存し、保持期間、削除、成功・失敗記録、リストア手順を管理する。
- Raspberry PiのCPU・メモリを実測し、バックテスト等の重い処理で常駐サービスを圧迫しないようにする。
- USB SSDを使用する場合は接続方式、電源、復旧手順を確認し、DB破損リスクを管理する。
- 再起動後に必要なサービスが手動操作なしで復旧することをテストする。

### 現在の注意点と次の運用確認

- 2026-07-11にJ-Quants銘柄マスターを件数制限なしで取得し、4,448銘柄を保存済み。
- `jquants-collector` はDocker Composeの常駐サービスで、1銘柄ずつ15秒以上の間隔で継続取得する。収集順は、取得可能な最新取引日を全銘柄で埋め、次に直近30取引日の欠損を新しい日付から補完し、その後に残りの約2年分を古い日付から補完する。
- 収集進捗は `price_collection_targets` と `price_collection_items`、保存済み価格は `market_prices` に保持する。ラズパイ・コンテナ再起動後は未取得分から再開する。
- 2026-07-12に、部分取得済みの日を日付全体の取得不能と誤判定していた進捗ロジックを修正済み。部分取得済みの2026-04-10は`active`へ復帰し、取得不能な銘柄だけを個別に除外して継続収集を確認した。
- J-Quants Free planの5件/分制限を安全に守るため、実効速度は最大4銘柄/分。4,448銘柄の最新取得可能日を埋める初回処理は約18時間半が目安。
- J-Quantsのcron実行は削除済みで、常駐サービスと二重実行しない。cronはFRED収集、短期分析、日次バックアップだけを担当する。
- バックアップジョブはSQLAlchemy URLを`pg_dump`互換URLへ変換するよう修正済み。ラズパイで正常にバックアップ作成を確認済み。
- DBマイグレーションはラズパイで `0004_price_collection_progress` まで適用済み。Mac側ではフェーズ2の `0005_spillover_features` を適用し、pytestは49件成功、Streamlitのヘルスチェックも成功した。ラズパイでは常駐コレクターの連続取得とStreamlit HTTP 200を確認済み。
- 2026-07-12にフェーズ2の最初の小タスクをMac側で実装した。米国前営業日のFRED終値リターンと、日本株・ETFの実J-Quants OHLCから、寄り付きギャップ・場中・日次リターンを別々に算出し、`spillover_features` に再実行可能な形で保存する。FREDの高値・安値・始値は推測しない。
- 同日に次の小タスクとして、米国前営業日の終値リターンを説明変数にするラグ単回帰と、将来データを混ぜないローリング回帰を追加した。結果は係数、p値、95%信頼区間、R²、サンプル数、ルール版とともに `spillover_model_results` へ保存する。Mac側のテストは52件成功し、`0006_spillover_model_results` のマイグレーションSQL生成を確認済み。
- さらにGranger検定を追加した。これは米国リターンの予測上の先行性を確認する検定であり、因果関係の証明として扱わない。30件未満では推定せず、ラグ別のF統計量・p値とルール版を `spillover_model_results` に保存する。DBスキーマ変更はなく、Mac側テストは53件成功した。
- 2026-07-12の合同評価で、sampleと実データの混在、非連続セッションの日次リターン扱い、調整価格、J-Quantsの恒久取得不能判定と収集方式をP0/P1課題と確認した。是正ToDoは `docs/17_remediation_todo.md` を正本とする。
- P0-1として、通常Compose起動からサンプル自動投入を削除し、Repositoryの通常分析経路は`sample`を除外するよう変更した。デモは`MARKET_DATA_MODE=demo`かつ`seed_sample_data.py --demo`で明示実行する。既存のsample行は削除せず、通常分析から隔離する。
- P0-1は開発部門エージェントの設計レビュー後にMacで確認済み。pytestは56件成功、`docker compose up -d --build`後のStreamlitヘルスチェックは`ok`、通常起動ログにsample seedの実行はない。Macのapp・db・jquants-collectorは起動中だが、Raspberry Piには一切反映していない。
- P0-2を開発部門エージェントの設計レビュー後にMacで実装・確認済み。`source_priority_v1` は指数・為替をFRED、日本株・日本ETFをJ-Quantsのみに固定し、未登録sourceを暗黙採用しない。選択済み入力のSHA-256、source方針、来歴、価格基準（P0-3確定前の暫定値）を分析結果に保存する。`0007_analysis_input_provenance` をMacへ適用済みで、旧相関結果20件は`requires_recalculation`へ安全に移行した。pytestは58件成功、Streamlitヘルスチェックも`ok`。
- 作業ツリーは多数の未コミット変更を含む（フェーズ1・2、収集、README/AGENTS/docs、P0-1をまとめた変更）。既存の変更を破棄・reset・checkoutしない。現時点の最新コミットは `5270a61 Limit PostgreSQL port to localhost`。
- 2026-07-12にラズパイを読み取り専用で確認した。ED25519指紋は保存済みの`SHA256:TZq+z1REo3s9bBcUcVGvhfDb5Hj6pqrH/vtuo37Ws6I`と一致。app・db・jquants-collectorは稼働中で、DBは`market_prices=5,238`、targetsは`active=2`／`unavailable=371`、itemsは`unavailable=270`だった。直近ログではJ-Quantsの429が`unavailable`として記録されている。状態は変更していない。
- P1-1をMac・ラズパイで完了。429・5xxは`retry_pending`、正常応答の空データだけは個別銘柄の`no_data`、401／403／入力不正は`error`として区別する。1銘柄の結果だけで日付全体を`unavailable`にしない。既存の`unavailable`項目は次回収集時に再試行対象へ戻す。実レスポンスでOHLCVがすべてnullの行を確認し、パーサーを`no_data`として扱うよう修正した。ラズパイで`13190`が`no_data`へ保存され、既存statusが`unavailable`から更新されることを確認した。Mac pytestは62件成功、ラズパイのHTTPヘルスチェックも`ok`。
- P0-3の第1段階をMacで実装・確認済み。外部取引カレンダーを導入せず、金曜→月曜以外で平日をまたぐ観測は`unverified_weekday_gap`相当として日次・ギャップから除外する。J-Quantsのraw／adjusted OHLC、adjustment factor、price basisを別々に保存する`0008_market_price_basis`をMacへ適用済み。pytestは66件成功、Streamlitヘルスチェックは`ok`。ラズパイへの配置と、公式取引カレンダー・連休累積の後続実装は未完了。
- ユーザー指定の将来要件を `docs/03_investment_analysis.md` と `docs/10_development_roadmap.md` に記録済み。フェーズ4で、短期・中期それぞれ初期250万円の独立した仮想口座を用意し、エントリー、利益確定、損切り後の現金・評価額・実現／未実現損益・残高推移を追跡する。現時点では実装しない。
- 2026-07-12にユーザー承認のもとラズパイへ配置した。事前バックアップは`market_signal_lab_20260712_062035.dump`。`0005_spillover_features`、`0006_spillover_model_results`、`0007_analysis_input_provenance`を適用し、app／db／jquants-collectorを再ビルド・再起動した。ラズパイ内pytestは60件成功、Streamlit HTTPヘルスチェックは`ok`。
- collectorは連続稼働中。J-QuantsのHTTP 200でOHLCVがすべてnullの銘柄`13190`は、個別`no_data`として保存され、日付全体は除外されなかった。itemsの集計は`no_data=1`、旧`unavailable=316`であり、新コードは次回収集時に旧項目を再試行対象として扱う。429／5xxの`retry_pending`は単体テスト済みで、実ログは継続監視する。
- 同日にP0-3をラズパイへ配置した。事前バックアップは`market_signal_lab_20260712_063934.dump`。`0008_market_price_basis`を適用し、`market_prices`へ`adjusted_open/high/low`、`adjustment_factor`、`price_basis`が存在することを確認した。ラズパイ内pytestは66件成功、Streamlit HTTPヘルスチェックは`ok`。collectorは成功1件（`15970`）と個別`no_data`を記録して連続稼働している。
- `15970`の既存価格行の`price_basis=legacy_unknown`は、J-Quants個別収集経路が調整済みOHLC、adjustment factor、price basisをupsert payloadへ渡していなかったことが原因だった。Mac側で修正と回帰テストを追加し、pytestは67件成功。2026-07-12にラズパイへ配置して再起動し、最新成功取得行`480A0`で`raw_ohlcv_with_adjusted`、調整済みOHLC、adjustment factorが保存されることを読み取り確認した。事前バックアップは`market_signal_lab_20260712_112442.dump`。
- 2026-07-12 20:25 JSTのラズパイ確認: ルートファイルシステムは114 GB中9.7 GB使用（空き100 GB、使用率9%）、PostgreSQLは19 MB、`market_prices`は6,245件。J-Quants収集は最新取得可能日の継続中で、targetsは`active=2`／`unavailable=371`、itemsは`no_data=134`／旧`unavailable=220`。429は`retry_pending`として再試行され、直後にHTTP 200成功を確認した。次の実装タスクは、公式取引カレンダーと連休中の累積USリターンを扱うP0-3後続設計である。P0/P1を完了するまで、ウォークフォワード検証を進めない。
- 2026-07-12: ラズパイ画面で、データ未取得時に短期分析が`KeyError: ['close']`となることを確認した。Mac側で空データを`データ不足`として扱う修正と回帰テストを追加し、pytestは68件成功、画面の起動確認も成功した。ユーザー指示により、この修正はラズパイへ未配置のままMac側で確認を継続する。ラズパイのデータ収集は継続中である。Codexの毎日10時のリマインドを作成し、Mac版の確認が一区切りついた後に、ラズパイ配置のタイミングを知らせる。
- P0-3後続の取引カレンダー候補を確認した。JPX・NYSEの公式休場日ページを正本とし、実行時のセッション判定にはApache-2.0の`exchange_calendars`を用いる案とする。Macの検証環境とPython 3.12コンテナで`XTKS`／`XNYS`の利用を確認済み。依存関係への追加、公式カレンダーとの差異検証、日米連休中の累積リターン実装はMac側の次タスクであり、ラズパイには未配置である。
- 合同会議の合意対応をMac側で進行中。`.dockerignore`を追加し、Dockerビルドコンテキストを約22KBへ縮小。app／collectorの起動時Alembic実行を分離し、明示マイグレーション手順へ変更。バックアップのpg_dump接続URLからパスワードを除外し、PGPASSFILEを使用。J-Quantsの最新日優先・履歴oldest-first補完順を修正。分析入力は調整済みOHLC優先、raw_*保持へ変更し、pytestは72件成功。いずれもラズパイ未配置。
- その後、公式取引カレンダーを相関・波及分析の実データ経路へ適用し、日本休場中の米国セッション累積を追加した。J-Quantsページングキーの追跡も追加し、Macで86970の2026-04-01〜2026-04-10を1回実測（8行、433ms）。Mac側のpytestは73件成功。バックアップ復元試験はapp側PostgreSQLクライアントで一時DBへ成功（assets=26、market_prices=53,689）。全期間のページング回数・429率、夏時間・分割の受け入れ試験、ラズパイ配置は未完了。
- 2026-08-15にユーザー承認のもと、直近連続履歴を優先する三段階収集キューをラズパイへ配置した。事前バックアップは`market_signal_lab_20260815_045321.dump`。Macの全pytestは110件、ラズパイの収集関連pytestは12件成功。`jquants-collector`だけを再作成し、`restart: unless-stopped`を維持した。ログで`latest:2026-05-15`完了後に`recent_gap:2026-05-14`、続いて`2026-05-13`へ移行したことを確認した。配置後は`market_prices=185,744`、最新日4,226件、2026-05-14は4,223件、ディスク使用率12%（114GB中14GB使用、96GB空き）。app・db・collectorはすべて稼働中で、DBスキーマ変更はない。
- 2026-08-15にMacの運用表示を改善した。`check_operations`は直近30営業日のカバー率、残件上限、15秒間隔による理論最短時間、最新collectorの`queue_phase`と対象日を保存する。「システム管理」は常駐collectorの大量ログと分離して最新運用確認を表示する。Mac復元DBではカバー率54.7%、残り60,705件、理論最短252.9時間、連続最大11営業日、30営業日到達0銘柄。localhostはMacスナップショットでありラズパイのリアルタイム値ではない。非表示タブの全銘柄感応度自動計算は停止し、明示切替にした。APIレート制限競合を避けるためMacのcollectorは停止し、Macの通常起動は`docker compose up --build db app`とする。ラズパイcollectorは継続稼働中。
- 2026-08-15にユーザー承認のもと、上記の運用表示をラズパイへ配置した。事前バックアップは`market_signal_lab_20260815_051900.dump`。DBマイグレーションは行わず、appだけを再起動し、dbとcollectorの連続稼働を維持した。ラズパイの関連pytestは21件成功、Streamlit HTTPヘルスチェックは`ok`。実画面の「システム管理」で`recent_gap:2026-04-24`、直近30営業日カバー率54.7%、完了日11/30、残り要求上限60,627件、理論最短252.6時間、最新連続履歴最大12営業日、30営業日到達0銘柄を確認した。確認時の`market_prices`は185,780件、過去24時間の失敗・再試行ジョブは0件、ディスク使用率11.52%（空き約102.8GB）。collectorは配置後もHTTP 200で次銘柄を継続取得している。
- 2026-08-15にMacのStreamlitを選択画面だけ実行するナビゲーションへ変更した。共通品質表示は軽量な状態取得へ分離し、非表示画面の分析・DB取得を行わない。未追跡だったデモ口座コードと合成ニュースproviderを保全・是正して正式接続し、短期・中期各250万円、次の東証営業日約定、手数料・スプレッド、利益確定・損切り、現金・評価額・実現／未実現損益・最大ドローダウン・残高推移をDB非保存で確認可能にした。Docker内pytestは122件成功、Macの初回デモ表示約4.0秒、口座計算約1.2秒、画面切替完了約1.9秒、HTTPヘルスチェック`ok`。DB変更はなく、ラズパイには未配置。

## 17. 完了報告の形式

- 実装概要
- 変更ファイル
- DB・マイグレーション変更
- 実行・運用方法
- テスト結果
- セキュリティ、データ品質、無料利用方針への影響
- 既知の制約と残課題
- 次フェーズ候補
